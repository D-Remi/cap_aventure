# CapAventure — Configuration environnement
# Copier ce fichier en .env et remplir les valeurs

# Base de données
DB_HOST=localhost
DB_PORT=3306
DB_NAME=capaventure
DB_USER=capaventure
DB_PASS=CapAv2024!

# JWT
JWT_SECRET=change-this-secret-in-production
JWT_EXPIRES=7d

# Email (optionnel — si vide, les emails ne sont pas envoyés)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER=ton@email.com
MAIL_PASS=ton-mot-de-passe-application
MAIL_FROM=CapAventure <ton@email.com>

# App
PORT=3001
NODE_ENV=production